query {
  apis: allExternalApis {
    layers {
      layer {
        name
        id
        url
        layer
        downloadUrl
        downloadLayer
        timeFilter
        columnFilter
      }
    }
    name
    id
  }
}
