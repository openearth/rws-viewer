query ($first: IntType, $skip: IntType = 0) {
  menus: allMenus(first: $first, skip: $skip) {
    id
    name
    children: layers {
      id
      name
      url
      layer
      downloadUrl
      downloadLayer
      tags {
        title
      }
      metadata {
        key: title
        value: content
      }
    }
    parent {
      id
    }
    metadata {
      key: title
      value: content
    }
  }
  _allMenusMeta {
    count
  }
}
